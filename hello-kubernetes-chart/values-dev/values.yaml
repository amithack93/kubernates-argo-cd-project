service:
  type: NodePort
  port: 80
  targetPort: 80
  nodePort: 30801

ingress:
  enabled: true
  annotations:
    kubernetes.io/ingress.class: nginx
  hosts:
    - host: dev.my-app.local
      paths:
        - /