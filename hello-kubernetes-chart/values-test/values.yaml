# values-test/values.yaml

service:
  type: ClusterIP
  port: 5000

# values-dev/values.yaml and values-test/values.yaml
ingress:
  enabled: true
  hosts:
    - host: test-app.local
      paths:
        - /